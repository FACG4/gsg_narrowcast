const express = require('express');
const path = require('path');
const exphbs = require('express-handlebars');
const fs = require('fs');
const addEvent = require ('AddEvent')

module.exports = {
  express, path, exphbs, fs,AddEvent,
};
